import { TypeCentreResponsabilite } from './type-centre-responsabilite';

describe('TypeCentreResponsabilite', () => {
  it('should create an instance', () => {
    expect(new TypeCentreResponsabilite()).toBeTruthy();
  });
});

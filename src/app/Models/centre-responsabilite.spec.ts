import { CentreResponsabilite } from './centre-responsabilite';

describe('CentreResponsabilite', () => {
  it('should create an instance', () => {
    expect(new CentreResponsabilite()).toBeTruthy();
  });
});
